fn main() {
    // initialize in memory wave buffers

    // begin timed loop to play the sound
    println!("Hello, world!");
}

fn init_buffers() -> [] {
    let n i32 = 1024;
    // we must use a vector
}
